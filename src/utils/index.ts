export enum ROUTES {
  Home = "/",
  Auth = "/auth",
  Denied = "/denied",
  NotFound = "*",
}

export enum LOADING_STATUS {
  PENDING = "PENDING",
  FULFILLED = "FULFILLED",
  REJECTED = "REJECTED",
}
